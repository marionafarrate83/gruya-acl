<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css" integrity="sha512-2SwdPD6INVrV/lHTZbO2nodKhrnDdJK9/kg2XD1r9uGqPo1cUbujc+IYdlYdEErWNu69gVcYgdxlmVmzTWnetw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://bootswatch.com/5/morph/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Control de Acceso Condominio</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <% if (user) { %>
                        <% if (user.role === 'administrador') { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/users">Usuarios</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/visitors/admin">
                                    <i class="fas fa-history"></i> Historial Visitas
                                </a>
                            </li>
                        <% } %>
                        <% if (user.role === 'guardia' || user.role === 'administrador') { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/visitors">Visitantes Hoy</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/visitors/scan">
                                    <i class="fas fa-qrcode"></i> Escanear QR
                                </a>
                            </li>
                        <% } %>
                        <% if (user.role === 'residente') { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/visitors/history">Mis Visitantes</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/visitors/create">Registrar Visitante</a>
                            </li>
                        <% } %>
                    <% } %>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button">
                                <%= user.email %> (<%= user.role %>)
                            </a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="/logout">Cerrar Sesión</a>
                            </div>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Iniciar Sesión</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Registrarse</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-4">