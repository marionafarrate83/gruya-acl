<%- include('../partials/header', { title: 'Visitante Registrado', user: user }) %>
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h4 class="text-center">¡Visitante Registrado Exitosamente!</h4>
            </div>
            <div class="card-body text-center">
                <div class="alert alert-success">
                    <h5>ID de Visita: <code><%= visitor.visitId %></code></h5>
                    <p>Nombre: <strong><%= visitor.visitorName %></strong></p>
                    <p>Fecha: <strong><%= visitor.visitDate.toLocaleDateString('es-ES') %> <%= visitor.visitTime %></strong></p>
                </div>
                
                <div class="qr-code mb-4">
                    <img src="<%= qrCodeData %>" alt="Código QR de la visita" class="img-fluid" style="max-width: 200px;">
                </div>

                <!-- Botones de PDF -->
                <div class="row mb-4">
                    <div class="col-md-4">
                        <div class="d-grid">
                            <button class="btn btn-outline-primary" 
                                    onclick="viewVisitPDF('<%= visitor._id %>')">
                                <i class="fas fa-eye"></i> Ver PDF
                            </button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <button class="btn btn-primary" 
                                    onclick="downloadVisitPDF('<%= visitor._id %>')">
                                <i class="fas fa-download"></i> Descargar
                            </button>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="d-grid">
                            <button class="btn btn-success" 
                                    onclick="shareVisitPDF('<%= visitor._id %>')">
                                <i class="fas fa-share-alt"></i> Compartir
                            </button>
                        </div>
                    </div>
                </div>

                <div class="alert alert-info">
                    <i class="fas fa-info-circle"></i> 
                    El PDF contiene toda la información de la visita y el código QR para presentar en la entrada.
                </div>

                <div class="d-flex justify-content-center gap-3 flex-wrap mt-3">
                    <a href="/visitors/create" class="btn btn-outline-primary">
                        <i class="fas fa-plus"></i> Nuevo Visitante
                    </a>
                    <a href="/visitors/history" class="btn btn-outline-secondary">
                        <i class="fas fa-history"></i> Ver Historial
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Incluir PDF Utils -->
<script src="/js/pdf-utils.js"></script>

<style>
.loading-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.7);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    color: white;
}

.loading-spinner {
    text-align: center;
}
</style>

<%- include('../partials/footer') %>