<%- include('../partials/header', { title: 'Crear Usuario', user: user }) %>
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h4 class="text-center">Crear Nuevo Usuario</h4>
            </div>
            <div class="card-body">
                <% if (error) { %>
                    <div class="alert alert-danger">
                        <%= error %>
                    </div>
                <% } %>
                
                <form action="/users/create" method="POST">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="residenceNumber">Número de Residencia</label>
                                <input type="text" class="form-control" id="residenceNumber" name="residenceNumber" 
                                       value="<%= formData ? formData.residenceNumber : '' %>" required>
                                <% if (errors && errors.find(e => e.path === 'residenceNumber')) { %>
                                    <small class="text-danger"><%= errors.find(e => e.path === 'residenceNumber').msg %></small>
                                <% } %>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="form-group">
                                <label for="role">Rol</label>
                                <select class="form-control" id="role" name="role" required>
                                    <option value="">Seleccionar rol</option>
                                    <option value="residente" <%= formData && formData.role === 'residente' ? 'selected' : '' %>>Residente</option>
                                    <option value="guardia" <%= formData && formData.role === 'guardia' ? 'selected' : '' %>>Guardia</option>
                                    <option value="administrador" <%= formData && formData.role === 'administrador' ? 'selected' : '' %>>Administrador</option>
                                </select>
                                <% if (errors && errors.find(e => e.path === 'role')) { %>
                                    <small class="text-danger"><%= errors.find(e => e.path === 'role').msg %></small>
                                <% } %>
                            </div>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="email">Correo Electrónico</label>
                        <input type="email" class="form-control" id="email" name="email" 
                               value="<%= formData ? formData.email : '' %>" required>
                        <% if (errors && errors.find(e => e.path === 'email')) { %>
                            <small class="text-danger"><%= errors.find(e => e.path === 'email').msg %></small>
                        <% } %>
                    </div>
                    
                    <div class="form-group">
                        <label for="phone">Número de Teléfono</label>
                        <input type="tel" class="form-control" id="phone" name="phone" 
                               value="<%= formData ? formData.phone : '' %>" required>
                        <% if (errors && errors.find(e => e.path === 'phone')) { %>
                            <small class="text-danger"><%= errors.find(e => e.path === 'phone').msg %></small>
                        <% } %>
                    </div>
                    
                    <div class="form-group">
                        <label for="password">Contraseña</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                        <% if (errors && errors.find(e => e.path === 'password')) { %>
                            <small class="text-danger"><%= errors.find(e => e.path === 'password').msg %></small>
                        <% } %>
                    </div>
                    
                    <div class="d-flex justify-content-between mt-4">
                        <a href="/users" class="btn btn-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-primary">Crear Usuario</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<%- include('../partials/footer') %>